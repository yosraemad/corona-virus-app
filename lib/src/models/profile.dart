class Profile {
  String preferredCountry;
  bool washedHands;
  bool socialDistanced;
  bool woreMask;

  Profile(
      {this.preferredCountry,
      this.socialDistanced,
      this.washedHands,
      this.woreMask});
}
